def call(String signtype, String certificate, String filestobesigned, String dirforsignedfiles)
{
if( ${signtype} == "openssl")
{
sh '''
#/bin/sh
#chmod +x /opt/signtooli/sign_tool.py
sudo python2 /opt/Signing.py $dirforsignedfiles $filestobesigned $certificate
'''
}
/*else
{
//windows
}
fi*/
